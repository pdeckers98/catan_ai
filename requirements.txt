# Core Catan engine + RL environment.
# NOTE: the Gymnasium env lives in the SEPARATE catanatron-gym package.
# Pin 4.0.0 — the 3.2.1 gym package depends on the legacy `gym` lib, which
# fails to build on Python 3.14. 4.0.0 uses gymnasium and pins it to 0.29.1.
catanatron==3.2.1
catanatron-gym==4.0.0
gymnasium==0.29.1   # pinned by catanatron-gym 4.0.0; SB3 2.9 works with it

# Deep RL (PyTorch under the hood)
torch>=2.12
# MaskablePPO (sb3-contrib) is the project's only trainer, src/agent/train.py,
# and also loads the shipped checkpoints for the `ppo` / `ppo-mcts` agent specs.
stable-baselines3==2.9.0
sb3-contrib==2.9.0

# Tooling & logging
flake8
pytest
wandb

# Phase 3 (colonist.io bridge). Drives the browser AND reads the game's
# WebSocket frames over CDP, so it is the whole transport, not just the clicker.
# After installing: playwright install chromium
playwright
